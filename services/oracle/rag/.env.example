# Oracle connection (via SSH tunnel: ssh -L 1521:localhost:1521 deploy@SERVER_IP)
ORACLE_HOST=localhost
ORACLE_PORT=1521
ORACLE_PDB=ENTW
ORACLE_USER=pdb_admin
ORACLE_PASS=OraclePdb2026

# Embedding model (runs locally via sentence-transformers)
EMBED_MODEL=all-MiniLM-L6-v2

# Ollama (local LLM)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5-coder:14b

# RAG parameters
CHUNK_SIZE=400
CHUNK_OVERLAP=50
TOP_K=5
