# apps/

Client applications that consume the Python API.

Planned:
- Flutter app (mobile + web via Cloudflare Pages)
- Demo web client

All clients connect exclusively through the Python API. Direct database access is not permitted. See [ADR-002](../docs/adr/002-api-only-db-access.md).

## Phase

Phase 2.
